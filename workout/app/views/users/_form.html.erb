<h1>Welcome to Workout!</h1>

<h2>Please create a name and password.</h2>

<%= form_for @user do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :password %>
<%= f.text_field :password %>

<%= f.submit %>

<% end %>
